# AGENT LEARNINGS (Modo Hermes)

Registro evolutivo de erros, soluções encontradas, otimizações e novas habilidades adquiridas de acordo com as experiências passadas.

## Lições Recentes
1. **09/04/2026:** Inicializado o Modo Hermes (Agente Autônomo de Nível Superior). O agente agora possui memória persistente baseada nestes arquivos no diretório de projeto atual.
2. **09/04/2026 (Upgrade):** Absorção de Matriz Omni-Skills (SEO, UX, AppSec, Backend e Copywriting de Vendas). 
   - *Ponto Crítico*: SEOPack ou scraping avançado foi refatorado. Agora obrigo a via `agent-browser` (via `firecrawl_browser_execute` ou CLI) com comandos `batch` e `auth vault` para driblar restrições e simular usuários perfeitamente em vez de raw `requests`.

## Registro de Erros
*(Nenhum erro reportado ainda)*
